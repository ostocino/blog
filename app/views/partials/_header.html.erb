 <!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation" ng-controller="NavCtrl">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#oscarchavezNav">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <%= link_to("oscarchavez", root_path , :class => "navbar-brand") %>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="oscarchavezNav">
                <ul class="nav navbar-nav navbar-right">
                  <li><%= link_to("about", about_path, :class => "navbar-brand") %></li>
                  <% if !user_signed_in? %>
                    <li><%= link_to("register", new_user_registration_path, :class => "navbar-brand")%></li>
                    <li><%= link_to("login", new_user_session_path , :class => "navbar-brand") %></li>
                    
                  <% else %>
                    <li><%= link_to(current_user.username, edit_user_registration_path) %></li>
                    <li><%= link_to("new post", posts_new_path, :class => "btn btn-primary") %></li>

                  <% end %>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>
